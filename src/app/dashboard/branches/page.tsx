import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

export default function BranchesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Branches</CardTitle>
        <CardDescription>Manage bank branches.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Branch management interface will be here.</p>
      </CardContent>
    </Card>
  );
}
