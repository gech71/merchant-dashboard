import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

export default function MerchantsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Merchants</CardTitle>
        <CardDescription>Manage company merchants.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Merchant management interface will be here.</p>
      </CardContent>
    </Card>
  );
}
